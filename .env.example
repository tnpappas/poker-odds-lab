# Poker Logic Lab has two apps with their own environment files:
#   apps/api/.env.example   (API, set these on Railway in production)
#   apps/web/.env.example   (frontend, set these on Vercel in production; VITE_ values are baked in at build time)
# Copy each to .env in its folder for local development. Nothing is required to run locally.
