# Copy to .env and fill in. Without DATABASE_URL the API runs with an
# in-memory store (data resets on restart) — handy for local development.

# Database (Neon Postgres)
DATABASE_URL=

# Auth (Clerk). Without CLERK_SECRET_KEY the API runs in dev-auth mode and
# trusts the `x-user-id` header (provisioning a user on the fly).
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=

# Payments (PayPal). When PAYPAL_CLIENT_ID + PAYPAL_SECRET are set, PayPal is
# the active processor (merchant of record = the account owner) and takes
# precedence over Polar. Get the Client ID/Secret from a REST app at
# developer.paypal.com. PAYPAL_WEBHOOK_ID comes from the webhook you create in
# the PayPal dashboard (needed to verify webhook signatures).
PAYPAL_CLIENT_ID=
PAYPAL_SECRET=
# 'live' for production, 'sandbox' for testing (defaults to live).
PAYPAL_ENV=live
PAYPAL_WEBHOOK_ID=
# Lifetime price in USD (defaults to 24.99 if unset).
PAYPAL_LIFETIME_PRICE=24.99

# Payments (Polar.sh) — fallback, used only when PayPal is not configured.
# Without POLAR_ACCESS_TOKEN and no PayPal, the payment routes return 501.
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
# 'sandbox' while testing, 'production' when live (defaults to production).
POLAR_SERVER=production
# Product IDs from your Polar dashboard (one per plan).
POLAR_LIFETIME_PRODUCT_ID=
POLAR_MONTHLY_PRODUCT_ID=
POLAR_ANNUAL_PRODUCT_ID=

# Observability (Sentry). Without SENTRY_DSN error tracking is disabled (no-op).
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0

# App
NODE_ENV=development
# FRONTEND_URL is the CORS allowlist (comma-separated for multiple origins) and
# the base URL for Stripe checkout redirects. In production, requests from
# origins not listed here are rejected.
PORT=3001
FRONTEND_URL=http://localhost:5173
